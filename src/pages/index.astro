---
import "../styles/tailwind.css";
import FileUploader from "../components/FileUploader"
import Logo from "../assets/wklogo.svg"


---
<html lang="hu" class="h-full dark">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>WebP Képkonvertáló – Gyors, Biztonságos, SEO-Optimalizált</title>
    <meta name="description" content="Professzionális WebP képkonvertáló 100% kliens oldali feldolgozással. Automatikus optimalizálás, magyar ékezet kezelés, 85-95% fájlméret csökkentés. Teljesen ingyenes." />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;500;600;700;800&display=swap" rel="stylesheet" />
    <style>
      :root {
        --color-primary: #0891B2;
        --color-accent: #F97316;
        --color-success: #10B981;
        --color-danger: #EF4444;
        --color-bg: #FAFAFA;
        --color-surface: #FFFFFF;
        --color-text: #0F172A;
        --color-text-muted: #64748B;
        --color-border: #E2E8F0;
        --font-display: 'Manrope', sans-serif;
        --font-body: 'Manrope', sans-serif;
        --radius: 16px;
        --shadow: 0 1px 3px rgba(0, 0, 0, 0.05);
        --shadow-lg: 0 20px 25px -5px rgba(0, 0, 0, 0.08), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
        --gradient-primary: linear-gradient(135deg, #0891B2 0%, #06B6D4 100%);
        --gradient-accent: linear-gradient(135deg, #F97316 0%, #FB923C 100%);
        --gradient-success: linear-gradient(135deg, #10B981 0%, #34D399 100%);
      }

      :root.dark {
        --color-primary: #939393;
        --color-accent: #FF4D30;
        --color-success: #FF4D30;
        --color-danger: #F87171;
        --color-bg: #0A0E15;
        --color-surface: #212631;
        --color-text: #F1F5F9;
        --color-text-muted: #939393;
        --color-border: #4a4a4a;
        --color-surface-elevated: #313131;
        --shadow: 0 1px 3px rgba(0, 0, 0, 0.3);
        --shadow-lg: 0 20px 25px -5px rgba(0, 0, 0, 0.5), 0 10px 10px -5px rgba(0, 0, 0, 0.3);
        --gradient-primary: linear-gradient(135deg, #4a4a4a 0%, #313131 100%);
        --gradient-accent: linear-gradient(135deg, #FF4D30 0%, #FF6B4D 100%);
        --gradient-success: linear-gradient(135deg, #FF4D30 0%, #FF6B4D 100%);
      }


      /* Smooth transitions for theme changes */
      * {
        transition: background-color 0.3s ease, border-color 0.3s ease, color 0.3s ease;
      }

      /* Micro-interactions */
      @keyframes fadeInUp {
        from {
          opacity: 0;
          transform: translateY(20px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      @keyframes scaleIn {
        from {
          opacity: 0;
          transform: scale(0.98) translateY(10px);
        }
        to {
          opacity: 1;
          transform: scale(1) translateY(0);
        }
      }

      @keyframes shimmer {
        0% {
          background-position: -1000px 0;
        }
        100% {
          background-position: 1000px 0;
        }
      }

      .animate-fade-in-up {
        animation: fadeInUp 0.6s ease-out forwards;
      }

      .animate-scale-in {
        animation: scaleIn 0.5s ease-out forwards;
      }

      .delay-100 { animation-delay: 0.1s; opacity: 0; }
      .delay-200 { animation-delay: 0.2s; opacity: 0; }
      .delay-300 { animation-delay: 0.3s; opacity: 0; }
      .delay-400 { animation-delay: 0.4s; opacity: 0; }
      .delay-500 { animation-delay: 0.5s; opacity: 0; }
      .delay-600 { animation-delay: 0.6s; opacity: 0; }

      /* Hero particles canvas */
      #hero-particles {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        z-index: 0;
        pointer-events: none;
      }

      .hero-container {
        position: relative;
        z-index: 1;
      }

      /* Bento card hover effects */
      .bento-card {
        transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
      }

      .bento-card:hover {
        transform: translateY(-2px);
        box-shadow: var(--shadow-lg);
      }

      .bento-card:hover .card-icon {
        transform: scale(1.05);
      }

      .card-icon {
        width: 48px;
        height: 48px;
        display: flex;
        align-items: center;
        justify-content: center;
        border-radius: 12px;
        transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
      }

      /* Button hover effects */
      .btn-primary {
        position: relative;
        overflow: hidden;
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      }

      .btn-primary::before {
        content: '';
        position: absolute;
        top: 0;
        left: -100%;
        width: 100%;
        height: 100%;
        background: linear-gradient(90deg, transparent, rgba(255,255,255,0.15), transparent);
        transition: left 0.6s ease;
      }

      .btn-primary:hover {
        transform: translateY(-2px);
        box-shadow: 0 8px 16px rgba(99, 102, 241, 0.3);
      }

      .btn-primary:active {
        transform: translateY(0);
      }

      .btn-primary:hover::before {
        left: 100%;
      }

      .btn-secondary {
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      }

      .btn-secondary:hover {
        transform: translateY(-2px);
        background-color: var(--color-secondary);
        color: var(--color-on-secondary);
        box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
      }

      .btn-secondary:active {
        transform: translateY(0);
      }

      /* Dark mode toggle */
      .theme-toggle {
        width: 56px;
        height: 28px;
        border-radius: 14px;
        background: var(--color-border);
        position: relative;
        cursor: pointer;
        transition: background 0.3s;
      }

      .theme-toggle:hover {
        background: var(--color-accent);
      }

      .theme-toggle::after {
        content: '🌙';
        position: absolute;
        width: 24px;
        height: 24px;
        border-radius: 50%;
        background: var(--color-surface);
        top: 2px;
        left: 2px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 12px;
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        box-shadow: var(--shadow);
      }

      :root.dark .theme-toggle::after {
        left: 30px;
        content: '☀️';
      }

      /* Gradient text */
      .gradient-text {
        background: var(--gradient-accent);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
      }

      .gradient-accent-text {
        background: var(--gradient-accent);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
      }

      /* Badge shimmer effect */
      .badge-shimmer {
        background: linear-gradient(90deg,
          var(--color-text-muted) 0%,
          var(--color-accent) 50%,
          var(--color-text-muted) 100%
        );
        background-size: 200% 100%;
        animation: shimmer 3s infinite;
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
      }

      /* Stats number animation */
      @keyframes countUp {
        from {
          opacity: 0;
          transform: translateY(20px) scale(0.9);
        }
        to {
          opacity: 1;
          transform: translateY(0) scale(1);
        }
      }

      .stat-number {
        animation: countUp 0.8s ease-out forwards;
      }

      /* FAQ accordion */
      .faq-item {
        transition: all 0.3s ease;
      }

      .faq-item:hover {
        background: var(--color-border);
      }

      /* Navigation link hover */
      .nav-link {
        position: relative;
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      }

      .nav-link::after {
        content: '';
        position: absolute;
        bottom: 0;
        left: 50%;
        transform: translateX(-50%);
        width: 0;
        height: 2px;
        background: var(--color-accent);
        transition: width 0.3s ease;
      }

      .nav-link:hover::after {
        width: 80%;
      }

      .nav-link:hover {
        color: var(--color-accent) !important;
        transform: translateY(-1px);
      }

      /* Comparison card interactions */
      .comparison-card .wp-section {
        transition: all 0.3s ease;
      }

      .comparison-card .astro-section {
        transition: all 0.3s ease;
      }

      .comparison-card:hover .wp-section {
        transform: translateX(-4px);
        opacity: 0.8;
      }

      .comparison-card:hover .astro-section {
        transform: translateX(4px);
        box-shadow: 0 4px 12px rgba(16, 185, 129, 0.2);
      }

      
    </style>
    <script>
      // Dark mode toggle
      const initTheme = () => {
        const savedTheme = localStorage.getItem('theme');
        const systemDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
        const html = document.documentElement;

        if (savedTheme === 'dark' || (!savedTheme && systemDark)) {
          html.classList.add('dark');
          html.classList.remove('light');
        } else if (savedTheme === 'light') {
          html.classList.add('light');
          html.classList.remove('dark');
        }
      };

      const toggleTheme = () => {
        const html = document.documentElement;
        const isDark = html.classList.contains('dark');

        if (isDark) {
          html.classList.remove('dark');
          html.classList.add('light');
          localStorage.setItem('theme', 'light');
        } else {
          html.classList.remove('light');
          html.classList.add('dark');
          localStorage.setItem('theme', 'dark');
        }
      };

      // Initialize on load
      initTheme();

      // Attach toggle handler
      document.addEventListener('DOMContentLoaded', () => {
        const toggle = document.getElementById('theme-toggle');
        if (toggle) {
          toggle.addEventListener('click', toggleTheme);
        }
      });
    </script>
    <script>
      // @ts-ignore
      import Alpine from 'alpinejs';
      // @ts-ignore
      import collapse from '@alpinejs/collapse';
      // @ts-ignore
      Alpine.plugin(collapse);
      // @ts-ignore
      window.Alpine = Alpine;
      Alpine.start();
    </script>
    <script>
      // @ts-ignore
      import Lenis from 'lenis';
      import 'lenis/dist/lenis.css';

      // Initialize Lenis smooth scroll
      // @ts-ignore
      const lenis = new Lenis({
        duration: 1.2,
        easing: (t) => Math.min(1, 1.001 - Math.pow(2, -10 * t)),
        orientation: 'vertical',
        gestureOrientation: 'vertical',
        smoothWheel: true,
        wheelMultiplier: 1,
        touchMultiplier: 2,
        infinite: false,
      });

      // Request animation frame loop
      // @ts-ignore
      function raf(time) {
        lenis.raf(time);
        requestAnimationFrame(raf);
      }

      requestAnimationFrame(raf);

      // Anchor link smooth scrolling
      document.addEventListener('DOMContentLoaded', () => {
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
          // @ts-ignore
          anchor.addEventListener('click', function (e) {
            // @ts-ignore
            const href = this.getAttribute('href');
            if (href && href !== '#') {
              e.preventDefault();
              const target = document.querySelector(href);
              if (target) {
                lenis.scrollTo(target, {
                  offset: -80,
                  duration: 1.5
                });
              }
            }
          });
        });
      });
    </script>
  </head>
  <body class="min-h-full antialiased" style="background-color: var(--color-bg); color: var(--color-text); font-family: var(--font-body);">
    <!-- Navigation -->
    <nav class="absolute  w-full top-0 z-50" style="" x-data="{ mobileMenuOpen: false }">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex items-center justify-end h-16">
         

          <!-- Desktop Nav -->
          <div class="hidden md:flex items-center gap-6">
            <a href="#features" class="nav-link text-sm font-semibold relative px-3 py-2 rounded-lg" style="color: var(--color-text);">
              Funkciók
            </a>
            <a href="#benefits" class="nav-link text-sm font-semibold relative px-3 py-2 rounded-lg" style="color: var(--color-text);">
              Előnyök
            </a>
            <a href="#faq" class="nav-link text-sm font-semibold relative px-3 py-2 rounded-lg" style="color: var(--color-text);">
              GYIK
            </a>
            <div id="theme-toggle" class="theme-toggle ml-2"></div>
          </div>

          <!-- Mobile Menu Button -->
          <button
            @click="mobileMenuOpen = !mobileMenuOpen"
            class="md:hidden flex flex-col gap-1.5 w-10 h-10 items-center justify-center rounded-lg hover:opacity-70 transition-opacity"
            style="background-color: var(--color-surface);"
          >
            <span class="w-5 h-0.5 transition-all" style="background-color: var(--color-text);" :class="{ 'rotate-45 translate-y-2': mobileMenuOpen }"></span>
            <span class="w-5 h-0.5 transition-all" style="background-color: var(--color-text);" :class="{ 'opacity-0': mobileMenuOpen }"></span>
            <span class="w-5 h-0.5 transition-all" style="background-color: var(--color-text);" :class="{ '-rotate-45 -translate-y-2': mobileMenuOpen }"></span>
          </button>
        </div>
      </div>

      <!-- Mobile Menu -->
      <div
        x-show="mobileMenuOpen"
        x-transition:enter="transition ease-out duration-200"
        x-transition:enter-start="opacity-0 scale-95"
        x-transition:enter-end="opacity-100 scale-100"
        x-transition:leave="transition ease-in duration-150"
        x-transition:leave-start="opacity-100 scale-100"
        x-transition:leave-end="opacity-0 scale-95"
        @click.away="mobileMenuOpen = false"
        class="md:hidden absolute left-0 right-0 px-4 pb-4"
        style="background-color: var(--color-bg); border-bottom: 1px solid var(--color-border); z-index: 100; top: 100%; transform-origin: top center;"
      >
        <div class="flex flex-col gap-2 py-4">
          <a href="#features" @click="mobileMenuOpen = false" class="px-4 py-3 rounded-lg text-sm font-medium transition-colors" style="background-color: var(--color-surface); color: var(--color-text);">Funkciók</a>
          <a href="#benefits" @click="mobileMenuOpen = false" class="px-4 py-3 rounded-lg text-sm font-medium transition-colors" style="background-color: var(--color-surface); color: var(--color-text);">Előnyök</a>
          <a href="#faq" @click="mobileMenuOpen = false" class="px-4 py-3 rounded-lg text-sm font-medium transition-colors" style="background-color: var(--color-surface); color: var(--color-text);">GYIK</a>
        </div>
      </div>
    </nav>

    <!-- Hero Section -->
    <section id="hero" class="hero pt-24 pb-16 px-4 sm:px-6 lg:px-8 relative overflow-hidden">
      <!-- Particle animation canvas -->
      <canvas id="hero-particles"></canvas>

      <div class="max-w-7xl mx-auto hero-container">
        <div class="grid lg:grid-cols-2 gap-12 items-start">
          <!-- Left: Text Content -->
          <div>
            <div class="inline-block px-4 py-2 rounded-full text-xs font-semibold mb-4 animate-fade-in-up" style="background-color: var(--color-surface); color: var(--color-primary); box-shadow: var(--shadow); border: 1px solid var(--color-border);">
              Kliens oldali feldolgozás
            </div>

            <h1 class="text-3xl md:text-4xl lg:text-5xl font-bold mb-4 animate-fade-in-up delay-100" style="font-family: var(--font-display); color: var(--color-text); line-height: 1.2;">
              WebP Képkonvertáló
            </h1>

            <p class="text-base md:text-lg mb-6 animate-fade-in-up delay-200 leading-relaxed" style="color: var(--color-text-muted);">
              Képek WebP-re konvertálása böngészőből. Automatikus optimalizálás, SEO-barát fájlnevek, 100% privát.
            </p>

            <!-- Key features list -->
            <ul class="space-y-2 animate-fade-in-up delay-300">
              <li class="flex items-center gap-2 text-sm" style="color: var(--color-text-muted);">
                <svg class="w-4 h-4 shrink-0" style="color: var(--color-primary);" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
                </svg>
                <span>85-95% fájlméret csökkenés</span>
              </li>
              <li class="flex items-center gap-2 text-sm" style="color: var(--color-text-muted);">
                <svg class="w-4 h-4 shrink-0" style="color: var(--color-primary);" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
                </svg>
                <span>Magyar ékezetek és szóközök kezelése</span>
              </li>
              <li class="flex items-center gap-2 text-sm" style="color: var(--color-text-muted);">
                <svg class="w-4 h-4 shrink-0" style="color: var(--color-primary);" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
                </svg>
                <span>Képek nem hagyják el a gépedet</span>
              </li>
              <li class="flex items-center gap-2 text-sm" style="color: var(--color-text-muted);">
                <svg class="w-4 h-4 shrink-0" style="color: var(--color-primary);" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
                </svg>
                <span>Akár 10 kép egyszerre, ZIP letöltés</span>
              </li>
            </ul>
          </div>

          <!-- Right: FileUploader Component -->
          <div class="animate-fade-in-up delay-400">
            <FileUploader client:load />
          </div>
        </div>
      </div>
    </section>

    <!-- Section Divider -->
    <div class="h-px w-full" style="background: linear-gradient(90deg, transparent 0%, var(--color-border) 50%, transparent 100%);"></div>

    <!-- Features Section -->
    <section id="features" class="py-20 px-4 sm:px-6 lg:px-8" style="background-color: var(--color-surface);">
      <div class="max-w-7xl mx-auto">
        <div class="text-center mb-12">
          <h2 class="text-2xl md:text-3xl font-bold mb-3" style="font-family: var(--font-display); color: var(--color-text);">
            Funkciók
          </h2>
          <p class="text-base md:text-lg max-w-2xl mx-auto" style="color: var(--color-text-muted);">
            Hogyan működik?
          </p>
        </div>

        <!-- Bento Grid -->
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
          <!-- Feature 1: Drag & Drop -->
          <div class="bento-card p-8 rounded-2xl" style="background-color: var(--color-bg); border: 1px solid var(--color-border); box-shadow: var(--shadow);">
            <div class="card-icon mb-4" style="background: var(--gradient-primary);">
              <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 16a4 4 0 01-.88-7.903A5 5 0 1115.9 6L16 6a5 5 0 011 9.9M15 13l-3-3m0 0l-3 3m3-3v12"/>
              </svg>
            </div>
            <h3 class="text-xl font-bold mb-3" style="font-family: var(--font-display); color: var(--color-text);">Drag & Drop</h3>
            <p class="leading-relaxed" style="color: var(--color-text-muted);">
              Húzd rá a képeket a feltöltő területre vagy kattints a fájlok kiválasztásához. Egyszerre akár 10 képet is feltölthetsz.
            </p>
          </div>

          <!-- Feature 2: Auto Optimization -->
          <div class="bento-card p-8 rounded-2xl" style="background-color: var(--color-bg); border: 1px solid var(--color-border); box-shadow: var(--shadow);">
            <div class="card-icon mb-4" style="background: var(--gradient-primary);">
              <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z"/>
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"/>
              </svg>
            </div>
            <h3 class="text-xl font-bold mb-3" style="font-family: var(--font-display); color: var(--color-text);">Automatikus optimalizálás</h3>
            <p class="leading-relaxed" style="color: var(--color-text-muted);">
              Max 1920px méret, 90% minőség, WebP formátum. Átlagosan 85-95% fájlméret csökkenés.
            </p>
          </div>

          <!-- Feature 3: SEO Names -->
          <div class="bento-card p-8 rounded-2xl" style="background-color: var(--color-bg); border: 1px solid var(--color-border); box-shadow: var(--shadow);">
            <div class="card-icon mb-4" style="background: var(--gradient-primary);">
              <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 7h.01M7 3h5c.512 0 1.024.195 1.414.586l7 7a2 2 0 010 2.828l-7 7a2 2 0 01-2.828 0l-7-7A1.994 1.994 0 013 12V7a4 4 0 014-4z"/>
              </svg>
            </div>
            <h3 class="text-xl font-bold mb-3" style="font-family: var(--font-display); color: var(--color-text);">SEO-barát fájlnevek</h3>
            <p class="leading-relaxed" style="color: var(--color-text-muted);">
              Automatikus magyar ékezetek és szóközök kezelése. "Kéménytető Szigetelés.jpg" → "kemenyteto-szigeteles.webp".
            </p>
          </div>

          <!-- Feature 4: Preview -->
          <div class="bento-card p-8 rounded-2xl" style="background-color: var(--color-bg); border: 1px solid var(--color-border); box-shadow: var(--shadow);">
            <div class="card-icon mb-4" style="background: var(--gradient-primary);">
              <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"/>
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"/>
              </svg>
            </div>
            <h3 class="text-xl font-bold mb-3" style="font-family: var(--font-display); color: var(--color-text);">Méret információk</h3>
            <p class="leading-relaxed" style="color: var(--color-text-muted);">
              Minden fájlnál látod az eredeti és konvertált méretet, a megtakarítás százalékát, és az összesített eredményt.
            </p>
          </div>

          <!-- Feature 5: Instant Download -->
          <div class="bento-card p-8 rounded-2xl" style="background-color: var(--color-bg); border: 1px solid var(--color-border); box-shadow: var(--shadow);">
            <div class="card-icon mb-4" style="background: var(--gradient-primary);">
              <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-4l-4 4m0 0l-4-4m4 4V4"/>
              </svg>
            </div>
            <h3 class="text-xl font-bold mb-3" style="font-family: var(--font-display); color: var(--color-text);">Letöltés</h3>
            <p class="leading-relaxed" style="color: var(--color-text-muted);">
              Egyenként vagy ZIP-ben letöltheted az összes optimalizált képet SEO-barát fájlnevekkel.
            </p>
          </div>

          <!-- Feature 6: Privacy -->
          <div class="bento-card p-8 rounded-2xl" style="background-color: var(--color-bg); border: 1px solid var(--color-border); box-shadow: var(--shadow);">
            <div class="card-icon mb-4" style="background: var(--gradient-primary);">
              <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"/>
              </svg>
            </div>
            <h3 class="text-xl font-bold mb-3" style="font-family: var(--font-display); color: var(--color-text);">Adatvédelem</h3>
            <p class="leading-relaxed" style="color: var(--color-text-muted);">
              Kliens oldali feldolgozás - a képek nem hagyják el a gépedet. Nincs feltöltés, nincs tárolás.
            </p>
          </div>
        </div>
      </div>
    </section>

    <!-- Section Divider -->
    <div class="h-px w-full" style="background: linear-gradient(90deg, transparent 0%, var(--color-border) 50%, transparent 100%);"></div>

    <!-- Benefits Section -->
    <section id="benefits" class="py-20 px-4 sm:px-6 lg:px-8">
      <div class="max-w-7xl mx-auto">
        <div class="text-center mb-12">
          <h2 class="text-2xl md:text-3xl font-bold mb-3" style="font-family: var(--font-display); color: var(--color-text);">
            Miért WebP?
          </h2>
        </div>

        <div class="grid md:grid-cols-2 gap-8">
          <!-- Technical Benefits -->
          <div class="p-8 rounded-2xl" style="background-color: var(--color-surface); border: 1px solid var(--color-border); box-shadow: var(--shadow);">
            <ul class="space-y-4">
              <li class="flex items-start gap-3">
                <svg class="w-5 h-5 shrink-0 mt-0.5" style="color: var(--color-primary);" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
                </svg>
                <div>
                  <strong style="color: var(--color-text);">25-35% kisebb</strong>
                  <span style="color: var(--color-text-muted);"> mint JPG ugyanazon minőségnél</span>
                </div>
              </li>
              <li class="flex items-start gap-3">
                <svg class="w-5 h-5 shrink-0 mt-0.5" style="color: var(--color-primary);" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
                </svg>
                <div>
                  <strong style="color: var(--color-text);">26% kisebb</strong>
                  <span style="color: var(--color-text-muted);"> mint PNG formátumnál</span>
                </div>
              </li>
              <li class="flex items-start gap-3">
                <svg class="w-5 h-5 shrink-0 mt-0.5" style="color: var(--color-primary);" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
                </svg>
                <div>
                  <strong style="color: var(--color-text);">Átlátszóság támogatás</strong>
                  <span style="color: var(--color-text-muted);"> mint PNG-nél</span>
                </div>
              </li>
              <li class="flex items-start gap-3">
                <svg class="w-5 h-5 shrink-0 mt-0.5" style="color: var(--color-primary);" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
                </svg>
                <div>
                  <strong style="color: var(--color-text);">Animáció támogatás</strong>
                  <span style="color: var(--color-text-muted);"> mint GIF-nél</span>
                </div>
              </li>
              <li class="flex items-start gap-3">
                <svg class="w-5 h-5 shrink-0 mt-0.5" style="color: var(--color-primary);" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
                </svg>
                <div>
                  <strong style="color: var(--color-text);">97%+ böngésző támogatás</strong>
                  <span style="color: var(--color-text-muted);"> globálisan</span>
                </div>
              </li>
            </ul>
          </div>

          <!-- Practical Benefits -->
          <div class="p-8 rounded-2xl" style="background-color: var(--color-surface); border: 1px solid var(--color-border); box-shadow: var(--shadow);">
            <ul class="space-y-4">
              <li class="flex items-start gap-3">
                <svg class="w-5 h-5 shrink-0 mt-0.5" style="color: var(--color-primary);" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
                </svg>
                <div>
                  <strong style="color: var(--color-text);">Kisebb tárhely igény</strong>
                  <span style="color: var(--color-text-muted);"> a szerveren</span>
                </div>
              </li>
              <li class="flex items-start gap-3">
                <svg class="w-5 h-5 shrink-0 mt-0.5" style="color: var(--color-primary);" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
                </svg>
                <div>
                  <strong style="color: var(--color-text);">Gyorsabb oldalbetöltés</strong>
                  <span style="color: var(--color-text-muted);"> kisebb fájlméret miatt</span>
                </div>
              </li>
              <li class="flex items-start gap-3">
                <svg class="w-5 h-5 shrink-0 mt-0.5" style="color: var(--color-primary);" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
                </svg>
                <div>
                  <strong style="color: var(--color-text);">Jobb SEO</strong>
                  <span style="color: var(--color-text-muted);"> a Core Web Vitals javulása miatt</span>
                </div>
              </li>
              <li class="flex items-start gap-3">
                <svg class="w-5 h-5 shrink-0 mt-0.5" style="color: var(--color-primary);" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
                </svg>
                <div>
                  <strong style="color: var(--color-text);">Kisebb sávszélesség</strong>
                  <span style="color: var(--color-text-muted);"> használat CDN esetén</span>
                </div>
              </li>
              <li class="flex items-start gap-3">
                <svg class="w-5 h-5 shrink-0 mt-0.5" style="color: var(--color-primary);" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
                </svg>
                <div>
                  <strong style="color: var(--color-text);">Mobilbarát</strong>
                  <span style="color: var(--color-text-muted);"> kisebb adatforgalom</span>
                </div>
              </li>
            </ul>
          </div>
        </div>
      </div>
    </section>

    <!-- Section Divider -->
    <div class="h-px w-full" style="background: linear-gradient(90deg, transparent 0%, var(--color-border) 50%, transparent 100%);"></div>

    <!-- FAQ Section -->
    <section id="faq" class="py-20 px-4 sm:px-6 lg:px-8 relative overflow-hidden" style="background-color: var(--color-surface);">
      <!-- Background decoration -->
      <div class="absolute inset-0 -z-10 opacity-20">
        <div class="absolute top-0 right-0 w-96 h-96 rounded-full blur-3xl" style="background: var(--gradient-primary);"></div>
        <div class="absolute bottom-0 left-0 w-72 h-72 rounded-full blur-3xl" style="background: var(--gradient-primary);"></div>
      </div>

      <div class="max-w-4xl mx-auto">
        <div class="text-center mb-12">
          <h2 class="text-2xl md:text-3xl font-bold mb-3" style="font-family: var(--font-display); color: var(--color-text);">
            Gyakori kérdések
          </h2>
          <p class="text-base md:text-lg" style="color: var(--color-text-muted);">
            Amit tudnod kell.
          </p>
        </div>

        <div class="space-y-3" x-data="{ openFaq: null }">
          <!-- FAQ 1 -->
          <div class="faq-item p-5 rounded-xl cursor-pointer backdrop-blur-sm" style="background: var(--color-surface); border: 1px solid var(--color-border); transition: all 0.3s ease;" @click="openFaq = openFaq === 1 ? null : 1">
            <div class="flex justify-between items-center gap-4">
              <h3 class="text-base font-bold" style="font-family: var(--font-display); color: var(--color-text);">Mennyibe kerül?</h3>
              <div class="flex-shrink-0 w-8 h-8 rounded-lg flex items-center justify-center" style="background: var(--gradient-primary);">
                <span class="text-white text-lg font-bold" x-text="openFaq === 1 ? '−' : '+'"></span>
              </div>
            </div>
            <div x-show="openFaq === 1" x-collapse>
              <p class="mt-3 text-sm leading-relaxed" style="color: var(--color-text-muted);">
                Ingyenes, open-source projekt. A teljes feldolgozás kliens oldalon fut a böngészőben, így nincs szerver költség.
              </p>
            </div>
          </div>

          <!-- FAQ 2 -->
          <div class="faq-item p-5 rounded-xl cursor-pointer backdrop-blur-sm" style="background: var(--color-surface); border: 1px solid var(--color-border); transition: all 0.3s ease;" @click="openFaq = openFaq === 2 ? null : 2">
            <div class="flex justify-between items-center gap-4">
              <h3 class="text-base font-bold" style="font-family: var(--font-display); color: var(--color-text);">Biztonságos? Hová kerülnek a képeim?</h3>
              <div class="flex-shrink-0 w-8 h-8 rounded-lg flex items-center justify-center" style="background: var(--gradient-primary);">
                <span class="text-white text-lg font-bold" x-text="openFaq === 2 ? '−' : '+'"></span>
              </div>
            </div>
            <div x-show="openFaq === 2" x-collapse>
              <p class="mt-3 text-sm leading-relaxed" style="color: var(--color-text-muted);">
                A feldolgozás teljesen kliens oldalon történik a böngésződben - a képek nem hagyják el a gépedet. Nincs szerver feltöltés, nincs tárolás.
              </p>
            </div>
          </div>

          <!-- FAQ 3 -->
          <div class="faq-item p-5 rounded-xl cursor-pointer backdrop-blur-sm" style="background: var(--color-surface); border: 1px solid var(--color-border); transition: all 0.3s ease;" @click="openFaq = openFaq === 3 ? null : 3">
            <div class="flex justify-between items-center gap-4">
              <h3 class="text-base font-bold" style="font-family: var(--font-display); color: var(--color-text);">Milyen formátumokat támogat?</h3>
              <div class="flex-shrink-0 w-8 h-8 rounded-lg flex items-center justify-center" style="background: var(--gradient-primary);">
                <span class="text-white text-lg font-bold" x-text="openFaq === 3 ? '−' : '+'"></span>
              </div>
            </div>
            <div x-show="openFaq === 3" x-collapse>
              <p class="mt-3 text-sm leading-relaxed" style="color: var(--color-text-muted);">
                Jelenleg JPG és PNG formátumokat. A konvertálás WebP formátumra történik.
              </p>
            </div>
          </div>

          <!-- FAQ 4 -->
          <div class="faq-item p-5 rounded-xl cursor-pointer backdrop-blur-sm" style="background: var(--color-surface); border: 1px solid var(--color-border); transition: all 0.3s ease;" @click="openFaq = openFaq === 4 ? null : 4">
            <div class="flex justify-between items-center gap-4">
              <h3 class="text-base font-bold" style="font-family: var(--font-display); color: var(--color-text);">Miért fontos a slug-osított fájlnév?</h3>
              <div class="flex-shrink-0 w-8 h-8 rounded-lg flex items-center justify-center" style="background: var(--gradient-primary);">
                <span class="text-white text-lg font-bold" x-text="openFaq === 4 ? '−' : '+'"></span>
              </div>
            </div>
            <div x-show="openFaq === 4" x-collapse>
              <p class="mt-3 text-sm leading-relaxed" style="color: var(--color-text-muted);">
                A Google kedveli az URL-barát fájlneveket. Slug-osított névvel ("kemenyteto.webp") jobb ranking képkeresésben és SEO szempontból is előnyös.
              </p>
            </div>
          </div>

          <!-- FAQ 5 -->
          <div class="faq-item p-5 rounded-xl cursor-pointer backdrop-blur-sm" style="background: var(--color-surface); border: 1px solid var(--color-border); transition: all 0.3s ease;" @click="openFaq = openFaq === 5 ? null : 5">
            <div class="flex justify-between items-center gap-4">
              <h3 class="text-base font-bold" style="font-family: var(--font-display); color: var(--color-text);">Működik mobilon?</h3>
              <div class="flex-shrink-0 w-8 h-8 rounded-lg flex items-center justify-center" style="background: var(--gradient-primary);">
                <span class="text-white text-lg font-bold" x-text="openFaq === 5 ? '−' : '+'"></span>
              </div>
            </div>
            <div x-show="openFaq === 5" x-collapse>
              <p class="mt-3 text-sm leading-relaxed" style="color: var(--color-text-muted);">
                Igen! Responsive design, mobil és tablet optimalizált. Minden modern böngészőben működik.
              </p>
            </div>
          </div>

          <!-- FAQ 6 -->
          <div class="faq-item p-5 rounded-xl cursor-pointer backdrop-blur-sm" style="background: var(--color-surface); border: 1px solid var(--color-border); transition: all 0.3s ease;" @click="openFaq = openFaq === 6 ? null : 6">
            <div class="flex justify-between items-center gap-4">
              <h3 class="text-base font-bold" style="font-family: var(--font-display); color: var(--color-text);">Hány képet tölthetek fel egyszerre?</h3>
              <div class="flex-shrink-0 w-8 h-8 rounded-lg flex items-center justify-center" style="background: var(--gradient-primary);">
                <span class="text-white text-lg font-bold" x-text="openFaq === 6 ? '−' : '+'"></span>
              </div>
            </div>
            <div x-show="openFaq === 6" x-collapse>
              <p class="mt-3 text-sm leading-relaxed" style="color: var(--color-text-muted);">
                Maximum 10 képet egyszerre! Drag & drop vagy kattintás után egyszerre konvertálhatsz és ZIP-ben is letöltheted az összeset.
              </p>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Footer -->
    <footer class="py-12 px-4 sm:px-6 lg:px-8" style="border-top: 1px solid var(--color-border); background-color: var(--color-bg);">
      <div class="max-w-7xl mx-auto">
        <div class="flex flex-col md:flex-row justify-between items-center gap-6">
          <!-- Brand & Copyright -->
          <div class="text-center md:text-left">
            <div class="text-xl font-bold mb-2" style="font-family: var(--font-display);">
              <span class="gradient-text">WEBP</span>Konvertáló
            </div>
            <p class="text-xs mb-1" style="color: var(--color-text-muted);">
              © {new Date().getFullYear()} — MIT License
            </p>
            <p class="text-xs" style="color: var(--color-text-muted);">
              Astro + React + TypeScript
            </p>
          </div>

          <!-- Privacy Statement -->
          <div class="text-center md:text-right">
            <p class="text-sm font-medium mb-1" style="color: var(--color-text);">
              100% Privát
            </p>
            <p class="text-xs max-w-xs" style="color: var(--color-text-muted);">
              Semmilyen adatot nem gyűjtök vagy tárolok. Kliens oldali feldolgozás.
            </p>
          </div>
        </div>

        <!-- Links -->
        <div class="flex justify-center gap-6 mt-8 pt-6" style="border-top: 1px solid var(--color-border);">
          <a href="#features" class="text-xs hover:opacity-70 transition-opacity" style="color: var(--color-text-muted);">Funkciók</a>
          <a href="#benefits" class="text-xs hover:opacity-70 transition-opacity" style="color: var(--color-text-muted);">Előnyök</a>
          <a href="#faq" class="text-xs hover:opacity-70 transition-opacity" style="color: var(--color-text-muted);">GYIK</a>
        </div>
      </div>
    </footer>

    <script is:inline>
      // @ts-nocheck
      // Hero Particles Animation
      (function() {
        const canvas = document.getElementById('hero-particles');
        if (!canvas) return;
        const ctx = canvas.getContext('2d');
        let particles = [];
        let mouse = { x: null, y: null, radius: 120 };
        let animationId;

        function resizeCanvas() {
          const hero = document.querySelector('.hero');
          if (!hero) return;
          canvas.width = hero.offsetWidth;
          canvas.height = hero.offsetHeight;
        }

        class Particle {
          constructor() {
            this.x = Math.random() * canvas.width;
            this.y = Math.random() * canvas.height;
            this.size = Math.random() * 2 + 1;
            this.baseX = this.x;
            this.baseY = this.y;
            this.density = Math.random() * 30 + 1;
            this.vx = (Math.random() - 0.5) * 0.3;
            this.vy = (Math.random() - 0.5) * 0.3;
            this.opacity = Math.random() * 0.5 + 0.2;
          }

          draw() {
            ctx.beginPath();
            ctx.arc(this.x, this.y, this.size, 0, Math.PI * 2);
            // Use primary color from CSS variable
            const isDark = document.documentElement.classList.contains('dark');
            const color = isDark ? '34, 211, 238' : '8, 145, 178';
            ctx.fillStyle = `rgba(${color}, ${this.opacity})`;
            ctx.fill();
          }

          update() {
            // Floating movement
            this.x += this.vx;
            this.y += this.vy;

            // Bounce off edges softly
            if (this.x < 0 || this.x > canvas.width) this.vx *= -1;
            if (this.y < 0 || this.y > canvas.height) this.vy *= -1;

            // Mouse interaction
            if (mouse.x !== null && mouse.y !== null) {
              let dx = mouse.x - this.x;
              let dy = mouse.y - this.y;
              let distance = Math.sqrt(dx * dx + dy * dy);

              if (distance < mouse.radius) {
                let force = (mouse.radius - distance) / mouse.radius;
                let dirX = dx / distance;
                let dirY = dy / distance;
                this.x -= dirX * force * 2;
                this.y -= dirY * force * 2;
              }
            }
          }
        }

        function init() {
          particles = [];
          const numberOfParticles = Math.floor((canvas.width * canvas.height) / 3000);
          for (let i = 0; i < Math.min(numberOfParticles, 300); i++) {
            particles.push(new Particle());
          }
        }

        function connectParticles() {
          const isDark = document.documentElement.classList.contains('dark');
          const color = isDark ? '34, 211, 238' : '8, 145, 178';

          for (let a = 0; a < particles.length; a++) {
            for (let b = a + 1; b < particles.length; b++) {
              let dx = particles[a].x - particles[b].x;
              let dy = particles[a].y - particles[b].y;
              let distance = Math.sqrt(dx * dx + dy * dy);

              if (distance < 100) {
                let opacity = (100 - distance) / 100 * 0.15;
                ctx.strokeStyle = `rgba(${color}, ${opacity})`;
                ctx.lineWidth = 0.5;
                ctx.beginPath();
                ctx.moveTo(particles[a].x, particles[a].y);
                ctx.lineTo(particles[b].x, particles[b].y);
                ctx.stroke();
              }
            }
          }
        }

        function animate() {
          ctx.clearRect(0, 0, canvas.width, canvas.height);

          particles.forEach(particle => {
            particle.update();
            particle.draw();
          });

          connectParticles();
          animationId = requestAnimationFrame(animate);
        }

        // Event Listeners
        const hero = document.querySelector('.hero');

        if (hero) {
          hero.addEventListener('mousemove', (e) => {
            const rect = hero.getBoundingClientRect();
            mouse.x = e.clientX - rect.left;
            mouse.y = e.clientY - rect.top;
          });

          hero.addEventListener('mouseleave', () => {
            mouse.x = null;
            mouse.y = null;
          });
        }

        window.addEventListener('resize', () => {
          resizeCanvas();
          init();
        });

        // Initialize
        resizeCanvas();
        init();
        animate();
      })();
    </script>
  </body>
</html>
