---
import "../styles/tailwind.css";
import FileUploader from "../components/FileUploader"


---
<html lang="hu" class="h-full">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>WebP Képkonvertáló – Gyors, Biztonságos, SEO-Optimalizált</title>
    <meta name="description" content="Professzionális WebP képkonvertáló 100% kliens oldali feldolgozással. Automatikus optimalizálás, magyar ékezet kezelés, 85-95% fájlméret csökkentés. Teljesen ingyenes." />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Syne:wght@400;500;600;700;800&family=Manrope:wght@400;500;600;700&display=swap" rel="stylesheet" />
    <style>
      :root {
        --color-primary: #0891B2;
        --color-accent: #F97316;
        --color-success: #10B981;
        --color-danger: #EF4444;
        --color-bg: #FAFAFA;
        --color-surface: #FFFFFF;
        --color-text: #0F172A;
        --color-text-muted: #64748B;
        --color-border: #E2E8F0;
        --font-display: 'Syne', sans-serif;
        --font-body: 'Manrope', sans-serif;
        --radius: 16px;
        --shadow: 0 1px 3px rgba(0, 0, 0, 0.05);
        --shadow-lg: 0 20px 25px -5px rgba(0, 0, 0, 0.08), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
        --gradient-primary: linear-gradient(135deg, #0891B2 0%, #06B6D4 100%);
        --gradient-accent: linear-gradient(135deg, #F97316 0%, #FB923C 100%);
        --gradient-success: linear-gradient(135deg, #10B981 0%, #34D399 100%);
      }

      :root.dark {
        --color-primary: #22D3EE;
        --color-accent: #FB923C;
        --color-success: #34D399;
        --color-danger: #F87171;
        --color-bg: #0F172A;
        --color-surface: #1E293B;
        --color-text: #F1F5F9;
        --color-text-muted: #94A3B8;
        --color-border: #334155;
        --shadow: 0 1px 3px rgba(0, 0, 0, 0.3);
        --shadow-lg: 0 20px 25px -5px rgba(0, 0, 0, 0.5), 0 10px 10px -5px rgba(0, 0, 0, 0.3);
        --gradient-primary: linear-gradient(135deg, #22D3EE 0%, #06B6D4 100%);
        --gradient-accent: linear-gradient(135deg, #FB923C 0%, #F97316 100%);
        --gradient-success: linear-gradient(135deg, #34D399 0%, #10B981 100%);
      }

      @media (prefers-color-scheme: dark) {
        :root:not(.light) {
          --color-primary: #22D3EE;
          --color-accent: #FB923C;
          --color-success: #34D399;
          --color-danger: #F87171;
          --color-bg: #0F172A;
          --color-surface: #1E293B;
          --color-text: #F1F5F9;
          --color-text-muted: #94A3B8;
          --color-border: #334155;
          --shadow: 0 1px 3px rgba(0, 0, 0, 0.3);
          --shadow-lg: 0 20px 25px -5px rgba(0, 0, 0, 0.5), 0 10px 10px -5px rgba(0, 0, 0, 0.3);
          --gradient-primary: linear-gradient(135deg, #22D3EE 0%, #06B6D4 100%);
          --gradient-accent: linear-gradient(135deg, #FB923C 0%, #F97316 100%);
          --gradient-success: linear-gradient(135deg, #34D399 0%, #10B981 100%);
        }
      }

      /* Smooth transitions for theme changes */
      * {
        transition: background-color 0.3s ease, border-color 0.3s ease, color 0.3s ease;
      }

      /* Micro-interactions */
      @keyframes fadeInUp {
        from {
          opacity: 0;
          transform: translateY(20px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      @keyframes scaleIn {
        from {
          opacity: 0;
          transform: scale(0.98) translateY(10px);
        }
        to {
          opacity: 1;
          transform: scale(1) translateY(0);
        }
      }

      @keyframes shimmer {
        0% {
          background-position: -1000px 0;
        }
        100% {
          background-position: 1000px 0;
        }
      }

      .animate-fade-in-up {
        animation: fadeInUp 0.6s ease-out forwards;
      }

      .animate-scale-in {
        animation: scaleIn 0.5s ease-out forwards;
      }

      .delay-100 { animation-delay: 0.1s; opacity: 0; }
      .delay-200 { animation-delay: 0.2s; opacity: 0; }
      .delay-300 { animation-delay: 0.3s; opacity: 0; }
      .delay-400 { animation-delay: 0.4s; opacity: 0; }
      .delay-500 { animation-delay: 0.5s; opacity: 0; }
      .delay-600 { animation-delay: 0.6s; opacity: 0; }

      /* Bento card hover effects */
      .bento-card {
        transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
      }

      .bento-card:hover {
        transform: translateY(-2px);
        box-shadow: var(--shadow-lg);
      }

      .bento-card:hover .card-icon {
        transform: scale(1.05);
      }

      .card-icon {
        width: 48px;
        height: 48px;
        display: flex;
        align-items: center;
        justify-content: center;
        border-radius: 12px;
        transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
      }

      /* Button hover effects */
      .btn-primary {
        position: relative;
        overflow: hidden;
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      }

      .btn-primary::before {
        content: '';
        position: absolute;
        top: 0;
        left: -100%;
        width: 100%;
        height: 100%;
        background: linear-gradient(90deg, transparent, rgba(255,255,255,0.15), transparent);
        transition: left 0.6s ease;
      }

      .btn-primary:hover {
        transform: translateY(-2px);
        box-shadow: 0 8px 16px rgba(99, 102, 241, 0.3);
      }

      .btn-primary:active {
        transform: translateY(0);
      }

      .btn-primary:hover::before {
        left: 100%;
      }

      .btn-secondary {
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      }

      .btn-secondary:hover {
        transform: translateY(-2px);
        background-color: var(--color-secondary);
        color: var(--color-on-secondary);
        box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
      }

      .btn-secondary:active {
        transform: translateY(0);
      }

      /* Dark mode toggle */
      .theme-toggle {
        width: 56px;
        height: 28px;
        border-radius: 14px;
        background: var(--color-border);
        position: relative;
        cursor: pointer;
        transition: background 0.3s;
      }

      .theme-toggle:hover {
        background: var(--color-primary);
      }

      .theme-toggle::after {
        content: '🌙';
        position: absolute;
        width: 24px;
        height: 24px;
        border-radius: 50%;
        background: var(--color-surface);
        top: 2px;
        left: 2px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 12px;
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        box-shadow: var(--shadow);
      }

      :root.dark .theme-toggle::after {
        left: 30px;
        content: '☀️';
      }

      /* Gradient text */
      .gradient-text {
        background: var(--gradient-primary);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
      }

      .gradient-accent-text {
        background: var(--gradient-accent);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
      }

      /* Badge shimmer effect */
      .badge-shimmer {
        background: linear-gradient(90deg,
          var(--color-primary) 0%,
          var(--color-accent) 50%,
          var(--color-primary) 100%
        );
        background-size: 200% 100%;
        animation: shimmer 3s infinite;
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
      }

      /* Stats number animation */
      @keyframes countUp {
        from {
          opacity: 0;
          transform: translateY(20px) scale(0.9);
        }
        to {
          opacity: 1;
          transform: translateY(0) scale(1);
        }
      }

      .stat-number {
        animation: countUp 0.8s ease-out forwards;
      }

      /* FAQ accordion */
      .faq-item {
        transition: all 0.3s ease;
      }

      .faq-item:hover {
        background: var(--color-border);
      }

      /* Navigation link hover */
      .nav-link {
        position: relative;
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      }

      .nav-link::after {
        content: '';
        position: absolute;
        bottom: 0;
        left: 50%;
        transform: translateX(-50%);
        width: 0;
        height: 2px;
        background: linear-gradient(90deg, var(--color-primary), var(--color-accent));
        transition: width 0.3s ease;
      }

      .nav-link:hover::after {
        width: 80%;
      }

      .nav-link:hover {
        color: var(--color-primary) !important;
        transform: translateY(-1px);
      }

      /* Comparison card interactions */
      .comparison-card .wp-section {
        transition: all 0.3s ease;
      }

      .comparison-card .astro-section {
        transition: all 0.3s ease;
      }

      .comparison-card:hover .wp-section {
        transform: translateX(-4px);
        opacity: 0.8;
      }

      .comparison-card:hover .astro-section {
        transform: translateX(4px);
        box-shadow: 0 4px 12px rgba(16, 185, 129, 0.2);
      }

      /* Dark mode navigation */
      :root.dark nav {
        background: linear-gradient(to bottom, var(--color-surface) 0%, rgba(30, 41, 59, 0.95) 100%) !important;
      }
    </style>
    <script>
      // Dark mode toggle
      const initTheme = () => {
        const savedTheme = localStorage.getItem('theme');
        const systemDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
        const html = document.documentElement;

        if (savedTheme === 'dark' || (!savedTheme && systemDark)) {
          html.classList.add('dark');
          html.classList.remove('light');
        } else if (savedTheme === 'light') {
          html.classList.add('light');
          html.classList.remove('dark');
        }
      };

      const toggleTheme = () => {
        const html = document.documentElement;
        const isDark = html.classList.contains('dark');

        if (isDark) {
          html.classList.remove('dark');
          html.classList.add('light');
          localStorage.setItem('theme', 'light');
        } else {
          html.classList.remove('light');
          html.classList.add('dark');
          localStorage.setItem('theme', 'dark');
        }
      };

      // Initialize on load
      initTheme();

      // Attach toggle handler
      document.addEventListener('DOMContentLoaded', () => {
        const toggle = document.getElementById('theme-toggle');
        if (toggle) {
          toggle.addEventListener('click', toggleTheme);
        }
      });
    </script>
    <script>
      // @ts-ignore
      import Alpine from 'alpinejs';
      // @ts-ignore
      window.Alpine = Alpine;
      Alpine.start();
    </script>
    <script>
      // @ts-ignore
      import Lenis from 'lenis';
      import 'lenis/dist/lenis.css';

      // Initialize Lenis smooth scroll
      // @ts-ignore
      const lenis = new Lenis({
        duration: 1.2,
        easing: (t) => Math.min(1, 1.001 - Math.pow(2, -10 * t)),
        orientation: 'vertical',
        gestureOrientation: 'vertical',
        smoothWheel: true,
        wheelMultiplier: 1,
        touchMultiplier: 2,
        infinite: false,
      });

      // Request animation frame loop
      // @ts-ignore
      function raf(time) {
        lenis.raf(time);
        requestAnimationFrame(raf);
      }

      requestAnimationFrame(raf);

      // Anchor link smooth scrolling
      document.addEventListener('DOMContentLoaded', () => {
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
          // @ts-ignore
          anchor.addEventListener('click', function (e) {
            // @ts-ignore
            const href = this.getAttribute('href');
            if (href && href !== '#') {
              e.preventDefault();
              const target = document.querySelector(href);
              if (target) {
                lenis.scrollTo(target, {
                  offset: -80,
                  duration: 1.5
                });
              }
            }
          });
        });
      });
    </script>
  </head>
  <body class="min-h-full antialiased" style="background-color: var(--color-bg); color: var(--color-text); font-family: var(--font-body);">
    <!-- Navigation -->
    <nav class="sticky top-0 z-50 backdrop-blur-xl" style="background: linear-gradient(to bottom, var(--color-surface) 0%, rgba(255, 255, 255, 0.95) 100%); border-bottom: 1px solid var(--color-border); box-shadow: var(--shadow);" x-data="{ mobileMenuOpen: false }">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex items-center justify-between h-16">
          <a href="/" class="text-xl font-bold transition-all duration-300 hover:opacity-80" style="font-family: var(--font-display); color: var(--color-text);">
            <span class="gradient-text">WEBP</span>Konvertáló
          </a>

          <!-- Desktop Nav -->
          <div class="hidden md:flex items-center gap-6">
            <a href="#features" class="nav-link text-sm font-semibold relative px-3 py-2 rounded-lg" style="color: var(--color-text);">
              Funkciók
            </a>
            <a href="#benefits" class="nav-link text-sm font-semibold relative px-3 py-2 rounded-lg" style="color: var(--color-text);">
              Előnyök
            </a>
            <a href="#faq" class="nav-link text-sm font-semibold relative px-3 py-2 rounded-lg" style="color: var(--color-text);">
              GYIK
            </a>
            <div id="theme-toggle" class="theme-toggle ml-2"></div>
          </div>

          <!-- Mobile Menu Button -->
          <button
            @click="mobileMenuOpen = !mobileMenuOpen"
            class="md:hidden flex flex-col gap-1.5 w-10 h-10 items-center justify-center rounded-lg hover:opacity-70 transition-opacity"
            style="background-color: var(--color-surface);"
          >
            <span class="w-5 h-0.5 transition-all" style="background-color: var(--color-text);" :class="{ 'rotate-45 translate-y-2': mobileMenuOpen }"></span>
            <span class="w-5 h-0.5 transition-all" style="background-color: var(--color-text);" :class="{ 'opacity-0': mobileMenuOpen }"></span>
            <span class="w-5 h-0.5 transition-all" style="background-color: var(--color-text);" :class="{ '-rotate-45 -translate-y-2': mobileMenuOpen }"></span>
          </button>
        </div>
      </div>

      <!-- Mobile Menu -->
      <div
        x-show="mobileMenuOpen"
        x-transition:enter="transition ease-out duration-200"
        x-transition:enter-start="opacity-0 scale-95"
        x-transition:enter-end="opacity-100 scale-100"
        x-transition:leave="transition ease-in duration-150"
        x-transition:leave-start="opacity-100 scale-100"
        x-transition:leave-end="opacity-0 scale-95"
        @click.away="mobileMenuOpen = false"
        class="md:hidden absolute left-0 right-0 px-4 pb-4"
        style="background-color: var(--color-bg); border-bottom: 1px solid var(--color-border); z-index: 100; top: 100%; transform-origin: top center;"
      >
        <div class="flex flex-col gap-2 py-4">
          <a href="#features" @click="mobileMenuOpen = false" class="px-4 py-3 rounded-lg text-sm font-medium transition-colors" style="background-color: var(--color-surface); color: var(--color-text);">Funkciók</a>
          <a href="#benefits" @click="mobileMenuOpen = false" class="px-4 py-3 rounded-lg text-sm font-medium transition-colors" style="background-color: var(--color-surface); color: var(--color-text);">Előnyök</a>
          <a href="#faq" @click="mobileMenuOpen = false" class="px-4 py-3 rounded-lg text-sm font-medium transition-colors" style="background-color: var(--color-surface); color: var(--color-text);">GYIK</a>
        </div>
      </div>
    </nav>

    <!-- Hero Section -->
    <section class="pt-16 pb-24 px-4 sm:px-6 lg:px-8 relative overflow-hidden">
      <!-- Background decoration -->
      <div class="absolute inset-0 -z-10 opacity-30">
        <div class="absolute top-20 left-10 w-72 h-72 rounded-full blur-3xl" style="background: var(--gradient-primary);"></div>
        <div class="absolute bottom-20 right-10 w-96 h-96 rounded-full blur-3xl" style="background: var(--gradient-accent);"></div>
      </div>

      <div class="max-w-7xl mx-auto">
        <div class="text-center max-w-4xl mx-auto mb-16">
          <div class="inline-block px-4 py-2 rounded-full text-xs font-semibold mb-6 animate-fade-in-up" style="background-color: var(--color-surface); color: var(--color-primary); box-shadow: var(--shadow); border: 1px solid var(--color-border);">
            Kliens oldali feldolgozás
          </div>

          <h1 class="text-5xl md:text-7xl font-bold mb-6 animate-fade-in-up delay-100" style="font-family: var(--font-display); color: var(--color-text); line-height: 1.05;">
            WebP Képkonvertáló
          </h1>

          <p class="text-lg md:text-xl mb-12 animate-fade-in-up delay-200 max-w-2xl mx-auto leading-relaxed" style="color: var(--color-text-muted);">
            Képek WebP-re konvertálása böngészőből.
          </p>
        </div>

        <!-- FileUploader Component -->
        <div class="max-w-3xl mx-auto animate-fade-in-up delay-400">
          <FileUploader client:load />
        </div>
      </div>
    </section>

    <!-- Features Section -->
    <section id="features" class="py-24 px-4 sm:px-6 lg:px-8" style="background-color: var(--color-surface);">
      <div class="max-w-7xl mx-auto">
        <div class="text-center mb-16">
          <h2 class="text-4xl md:text-5xl font-bold mb-4" style="font-family: var(--font-display); color: var(--color-text);">
            Funkciók
          </h2>
          <p class="text-lg md:text-xl max-w-2xl mx-auto" style="color: var(--color-text-muted);">
            Amit tud az alkalmazás.
          </p>
        </div>

        <!-- Bento Grid -->
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
          <!-- Feature 1: Drag & Drop -->
          <div class="bento-card p-8 rounded-2xl" style="background-color: var(--color-bg); border: 2px solid var(--color-border); box-shadow: var(--shadow);">
            <div class="card-icon mb-4" style="background: var(--gradient-primary);">
              <span class="text-3xl">📤</span>
            </div>
            <h3 class="text-xl font-bold mb-3" style="font-family: var(--font-display); color: var(--color-text);">Drag & Drop</h3>
            <p class="leading-relaxed" style="color: var(--color-text-muted);">
              Húzd rá a képet a feltöltő területre vagy kattints a fájl kiválasztásához.
            </p>
          </div>

          <!-- Feature 2: Auto Optimization -->
          <div class="bento-card p-8 rounded-2xl" style="background-color: var(--color-bg); border: 2px solid var(--color-border); box-shadow: var(--shadow);">
            <div class="card-icon mb-4" style="background: var(--gradient-accent);">
              <span class="text-3xl">🔧</span>
            </div>
            <h3 class="text-xl font-bold mb-3" style="font-family: var(--font-display); color: var(--color-text);">Automatikus optimalizálás</h3>
            <p class="leading-relaxed" style="color: var(--color-text-muted);">
              Max 1920px méret, 90% minőség, WebP formátum. Átlagosan 85-95% fájlméret csökkenés.
            </p>
          </div>

          <!-- Feature 3: SEO Names -->
          <div class="bento-card p-8 rounded-2xl" style="background-color: var(--color-bg); border: 2px solid var(--color-border); box-shadow: var(--shadow);">
            <div class="card-icon mb-4" style="background: var(--gradient-success);">
              <span class="text-3xl">📝</span>
            </div>
            <h3 class="text-xl font-bold mb-3" style="font-family: var(--font-display); color: var(--color-text);">SEO-barát fájlnevek</h3>
            <p class="leading-relaxed" style="color: var(--color-text-muted);">
              Automatikus magyar ékezet normalizálás. "Kéménytető.jpg" → "kemenyteto.webp".
            </p>
          </div>

          <!-- Feature 4: Preview -->
          <div class="bento-card p-8 rounded-2xl" style="background-color: var(--color-bg); border: 2px solid var(--color-border); box-shadow: var(--shadow);">
            <div class="card-icon mb-4" style="background: var(--gradient-primary);">
              <span class="text-3xl">👀</span>
            </div>
            <h3 class="text-xl font-bold mb-3" style="font-family: var(--font-display); color: var(--color-text);">Előnézet</h3>
            <p class="leading-relaxed" style="color: var(--color-text-muted);">
              Eredeti és konvertált kép megjelenítése méret és százalékos megtakarítás adatokkal.
            </p>
          </div>

          <!-- Feature 5: Instant Download -->
          <div class="bento-card p-8 rounded-2xl" style="background-color: var(--color-bg); border: 2px solid var(--color-border); box-shadow: var(--shadow);">
            <div class="card-icon mb-4" style="background: var(--gradient-accent);">
              <span class="text-3xl">💾</span>
            </div>
            <h3 class="text-xl font-bold mb-3" style="font-family: var(--font-display); color: var(--color-text);">Letöltés</h3>
            <p class="leading-relaxed" style="color: var(--color-text-muted);">
              Egy kattintással letölthető az optimalizált kép SEO-barát fájlnévvel.
            </p>
          </div>

          <!-- Feature 6: Privacy -->
          <div class="bento-card p-8 rounded-2xl" style="background-color: var(--color-bg); border: 2px solid var(--color-border); box-shadow: var(--shadow);">
            <div class="card-icon mb-4" style="background: var(--gradient-success);">
              <span class="text-3xl">🔒</span>
            </div>
            <h3 class="text-xl font-bold mb-3" style="font-family: var(--font-display); color: var(--color-text);">Adatvédelem</h3>
            <p class="leading-relaxed" style="color: var(--color-text-muted);">
              Kliens oldali feldolgozás - a képek nem hagyják el a gépedet. Nincs feltöltés, nincs tárolás.
            </p>
          </div>
        </div>
      </div>
    </section>

    <!-- Benefits Section -->
    <section id="benefits" class="py-24 px-4 sm:px-6 lg:px-8">
      <div class="max-w-7xl mx-auto">
        <div class="text-center mb-16">
          <h2 class="text-4xl md:text-5xl font-bold mb-4" style="font-family: var(--font-display); color: var(--color-text);">
            Miért WebP?
          </h2>
          <p class="text-lg md:text-xl max-w-2xl mx-auto" style="color: var(--color-text-muted);">
            Technikai előnyök és valós eredmények.
          </p>
        </div>

        <div class="grid md:grid-cols-2 gap-8 mb-12">
          <!-- Technical Benefits -->
          <div class="p-8 rounded-2xl" style="background-color: var(--color-surface); border: 2px solid var(--color-border); box-shadow: var(--shadow);">
            <h3 class="text-2xl font-bold mb-6" style="font-family: var(--font-display); color: var(--color-text);">
              Technikai előnyök
            </h3>
            <ul class="space-y-4">
              <li class="flex items-start gap-3">
                <span class="text-xl">•</span>
                <div>
                  <strong style="color: var(--color-text);">25-35% kisebb</strong>
                  <span style="color: var(--color-text-muted);"> mint JPG ugyanazon minőségnél</span>
                </div>
              </li>
              <li class="flex items-start gap-3">
                <span class="text-xl">•</span>
                <div>
                  <strong style="color: var(--color-text);">26% kisebb</strong>
                  <span style="color: var(--color-text-muted);"> mint PNG formátumnál</span>
                </div>
              </li>
              <li class="flex items-start gap-3">
                <span class="text-xl">•</span>
                <div>
                  <strong style="color: var(--color-text);">Átlátszóság támogatás</strong>
                  <span style="color: var(--color-text-muted);"> mint PNG-nél</span>
                </div>
              </li>
              <li class="flex items-start gap-3">
                <span class="text-xl">•</span>
                <div>
                  <strong style="color: var(--color-text);">Animáció támogatás</strong>
                  <span style="color: var(--color-text-muted);"> mint GIF-nél</span>
                </div>
              </li>
              <li class="flex items-start gap-3">
                <span class="text-xl">•</span>
                <div>
                  <strong style="color: var(--color-text);">97%+ böngésző támogatás</strong>
                  <span style="color: var(--color-text-muted);"> globálisan</span>
                </div>
              </li>
            </ul>
          </div>

          <!-- Practical Benefits -->
          <div class="p-8 rounded-2xl" style="background-color: var(--color-surface); border: 2px solid var(--color-border); box-shadow: var(--shadow);">
            <h3 class="text-2xl font-bold mb-6" style="font-family: var(--font-display); color: var(--color-text);">
              Gyakorlati előnyök
            </h3>
            <ul class="space-y-4">
              <li class="flex items-start gap-3">
                <span class="text-xl">•</span>
                <div>
                  <strong style="color: var(--color-text);">Kisebb tárhely igény</strong>
                  <span style="color: var(--color-text-muted);"> a szerveren</span>
                </div>
              </li>
              <li class="flex items-start gap-3">
                <span class="text-xl">•</span>
                <div>
                  <strong style="color: var(--color-text);">Gyorsabb oldalbetöltés</strong>
                  <span style="color: var(--color-text-muted);"> kisebb fájlméret miatt</span>
                </div>
              </li>
              <li class="flex items-start gap-3">
                <span class="text-xl">•</span>
                <div>
                  <strong style="color: var(--color-text);">Jobb SEO</strong>
                  <span style="color: var(--color-text-muted);"> a Core Web Vitals javulása miatt</span>
                </div>
              </li>
              <li class="flex items-start gap-3">
                <span class="text-xl">•</span>
                <div>
                  <strong style="color: var(--color-text);">Kisebb sávszélesség</strong>
                  <span style="color: var(--color-text-muted);"> használat CDN esetén</span>
                </div>
              </li>
              <li class="flex items-start gap-3">
                <span class="text-xl">•</span>
                <div>
                  <strong style="color: var(--color-text);">Mobilbarát</strong>
                  <span style="color: var(--color-text-muted);"> kisebb adatforgalom</span>
                </div>
              </li>
            </ul>
          </div>
        </div>

   
      </div>
    </section>

    <!-- FAQ Section -->
    <section id="faq" class="py-24 px-4 sm:px-6 lg:px-8">
      <div class="max-w-4xl mx-auto">
        <div class="text-center mb-16">
          <h2 class="text-4xl md:text-5xl font-bold mb-4" style="font-family: var(--font-display); color: var(--color-text);">
            Gyakori kérdések
          </h2>
        </div>

        <div class="space-y-4" x-data="{ openFaq: null }">
          <!-- FAQ 1 -->
          <div class="faq-item p-6 rounded-xl cursor-pointer" style="background-color: var(--color-surface); border: 2px solid var(--color-border);" @click="openFaq = openFaq === 1 ? null : 1">
            <div class="flex justify-between items-center">
              <h3 class="text-lg font-bold" style="font-family: var(--font-display); color: var(--color-text);">Mennyibe kerül?</h3>
              <span class="text-2xl" style="color: var(--color-primary);" x-text="openFaq === 1 ? '−' : '+'"></span>
            </div>
            <div x-show="openFaq === 1" x-collapse>
              <p class="mt-4" style="color: var(--color-text-muted);">
                Ingyenes, open-source projekt. A teljes feldolgozás kliens oldalon fut a böngészőben, így nincs szerver költség.
              </p>
            </div>
          </div>

          <!-- FAQ 2 -->
          <div class="faq-item p-6 rounded-xl cursor-pointer" style="background-color: var(--color-surface); border: 2px solid var(--color-border);" @click="openFaq = openFaq === 2 ? null : 2">
            <div class="flex justify-between items-center">
              <h3 class="text-lg font-bold" style="font-family: var(--font-display); color: var(--color-text);">Biztonságos? Hová kerülnek a képeim?</h3>
              <span class="text-2xl" style="color: var(--color-primary);" x-text="openFaq === 2 ? '−' : '+'"></span>
            </div>
            <div x-show="openFaq === 2" x-collapse>
              <p class="mt-4" style="color: var(--color-text-muted);">
                A feldolgozás teljesen kliens oldalon történik a böngésződben - a képek nem hagyják el a gépedet. Nincs szerver feltöltés, nincs tárolás.
              </p>
            </div>
          </div>

          <!-- FAQ 3 -->
          <div class="faq-item p-6 rounded-xl cursor-pointer" style="background-color: var(--color-surface); border: 2px solid var(--color-border);" @click="openFaq = openFaq === 3 ? null : 3">
            <div class="flex justify-between items-center">
              <h3 class="text-lg font-bold" style="font-family: var(--font-display); color: var(--color-text);">Milyen formátumokat támogat?</h3>
              <span class="text-2xl" style="color: var(--color-primary);" x-text="openFaq === 3 ? '−' : '+'"></span>
            </div>
            <div x-show="openFaq === 3" x-collapse>
              <p class="mt-4" style="color: var(--color-text-muted);">
                Jelenleg JPG és PNG formátumokat. A konvertálás WebP formátumra történik. Hamarosan érkezik: GIF, BMP, TIFF támogatás.
              </p>
            </div>
          </div>

          <!-- FAQ 4 -->
          <div class="faq-item p-6 rounded-xl cursor-pointer" style="background-color: var(--color-surface); border: 2px solid var(--color-border);" @click="openFaq = openFaq === 4 ? null : 4">
            <div class="flex justify-between items-center">
              <h3 class="text-lg font-bold" style="font-family: var(--font-display); color: var(--color-text);">Miért fontos a slug-osított fájlnév?</h3>
              <span class="text-2xl" style="color: var(--color-primary);" x-text="openFaq === 4 ? '−' : '+'"></span>
            </div>
            <div x-show="openFaq === 4" x-collapse>
              <p class="mt-4" style="color: var(--color-text-muted);">
                A Google kedveli az URL-barát fájlneveket. Magyar ékezetekkel ("Kéménytető.jpg") a képkeresés nehezebben találja meg a képedet. Slug-osított névvel ("kemenyteto.webp") jobb ranking képkeresésben és SEO szempontból is előnyös.
              </p>
            </div>
          </div>

          <!-- FAQ 5 -->
          <div class="faq-item p-6 rounded-xl cursor-pointer" style="background-color: var(--color-surface); border: 2px solid var(--color-border);" @click="openFaq = openFaq === 5 ? null : 5">
            <div class="flex justify-between items-center">
              <h3 class="text-lg font-bold" style="font-family: var(--font-display); color: var(--color-text);">Működik mobilon?</h3>
              <span class="text-2xl" style="color: var(--color-primary);" x-text="openFaq === 5 ? '−' : '+'"></span>
            </div>
            <div x-show="openFaq === 5" x-collapse>
              <p class="mt-4" style="color: var(--color-text-muted);">
                Igen! Responsive design, mobil és tablet optimalizált. Minden modern böngészőben működik: Chrome, Firefox, Safari, Edge.
              </p>
            </div>
          </div>

          <!-- FAQ 6 -->
          <div class="faq-item p-6 rounded-xl cursor-pointer" style="background-color: var(--color-surface); border: 2px solid var(--color-border);" @click="openFaq = openFaq === 6 ? null : 6">
            <div class="flex justify-between items-center">
              <h3 class="text-lg font-bold" style="font-family: var(--font-display); color: var(--color-text);">Hány képet tölthetek fel egyszerre?</h3>
              <span class="text-2xl" style="color: var(--color-primary);" x-text="openFaq === 6 ? '−' : '+'"></span>
            </div>
            <div x-show="openFaq === 6" x-collapse>
              <p class="mt-4" style="color: var(--color-text-muted);">
                Jelenleg 1 kép egyszerre. Batch feldolgozás funkció (több fájl egyszerre, ZIP letöltés) tervben van.
              </p>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Roadmap Section -->
    <section class="py-24 px-4 sm:px-6 lg:px-8" style="background-color: var(--color-surface);">
      <div class="max-w-6xl mx-auto">
        <div class="text-center mb-16">
          <h2 class="text-4xl md:text-5xl font-bold mb-4" style="font-family: var(--font-display); color: var(--color-text);">
            Tervezett funkciók
          </h2>
          <p class="text-lg md:text-xl" style="color: var(--color-text-muted);">
            Amit még szeretnék hozzáadni.
          </p>
        </div>

        <div class="grid md:grid-cols-2 gap-6">
          <!-- Q1 2026 -->
          <div class="p-8 rounded-2xl" style="background-color: var(--color-bg); border: 2px solid var(--color-primary);">
         
            <h3 class="text-2xl font-bold mb-4" style="font-family: var(--font-display); color: var(--color-text);">Batch & Progress</h3>
            <ul class="space-y-2" style="color: var(--color-text-muted);">
              <li>✓ Batch feldolgozás (max 10 fájl)</li>
              <li>✓ ZIP letöltés (összes konvertált)</li>
              <li>✓ Progress bar vizuális feedback</li>
            </ul>
          </div>

          <!-- Q2 2026 -->
          <div class="p-8 rounded-2xl" style="background-color: var(--color-bg); border: 2px solid var(--color-accent);">
         
            <h3 class="text-2xl font-bold mb-4" style="font-family: var(--font-display); color: var(--color-text);">Egyedi Beállítások</h3>
            <ul class="space-y-2" style="color: var(--color-text-muted);">
              <li>✓ Egyedi méretezés UI</li>
              <li>✓ Képarány választó (16:9, 4:3, 1:1)</li>
              <li>✓ Minőség slider (0-100%)</li>
              <li>✓ Crop funkció</li>
            </ul>
          </div>

       

       
        </div>
      </div>
    </section>



    <!-- Footer -->
    <footer class="py-16 px-4 sm:px-6 lg:px-8" style="border-top: 1px solid var(--color-border); background-color: var(--color-surface);">
      <div class="max-w-7xl mx-auto">
        <div class="grid md:grid-cols-2 gap-12 mb-12">
          <!-- Brand -->
          <div>
            <div class="text-2xl font-bold mb-4" style="font-family: var(--font-display);">
              <span class="gradient-text">WEBP</span>Konvertáló
            </div>
            <p class="text-sm leading-relaxed" style="color: var(--color-text-muted);">
              Képkonvertálás WebP formátumra böngészőből.
            </p>
        
          <div class="text-sm" style="color: var(--color-text-muted);">
            © {new Date().getFullYear()} WEBPKonvertáló
          </div>
          <div class="flex items-center gap-2 text-sm" style="color: var(--color-text-muted);">
            <span class="font-semibold">Semmilyen adatot nem gyűjtök vagy tárolok Rólad :)</span>
        
        </div>
          </div>

         
          

          <!-- Legal -->
       

        <!-- Bottom Bar -->
      
      </div>
    </footer>
  </body>
</html>
